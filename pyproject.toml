[project]
name = "feedercomp"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "customtkinter",
    "reportlab",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "ruff",
    "pyinstaller",
]
