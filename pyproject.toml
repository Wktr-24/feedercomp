[project]
name = "feedercomp"
version = "0.4.0"
requires-python = ">=3.10"
dependencies = [
    "customtkinter",
    "reportlab",
]

[tool.setuptools.packages.find]
include = ["app*"]

[project.optional-dependencies]
dev = [
    "pytest",
    "ruff",
    "pyinstaller",
]
