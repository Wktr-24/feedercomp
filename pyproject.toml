[project]
name = "feedercomp"
version = "0.2.1"
requires-python = ">=3.10"
dependencies = [
    "customtkinter",
    "reportlab",
]

[tool.setuptools.packages.find]
include = ["app*"]

[project.optional-dependencies]
dev = [
    "pytest",
    "ruff",
    "pyinstaller",
]
